# 随机翻牌效果网页应用设计文档

## 项目概述

本项目是一个模拟翻牌效果的网页应用，用户可以通过点击卡片查看随机内容。卡片默认展示背面，鼠标悬停时有高亮浮动效果，点击时有翻转特效并展示正面内容。卡片正面由背景和三个字符框体组成，框体样式通过配置文件维护，内容从CSV数据文件中随机获取。

## 文件结构

```
randomcard/
├── index.html              # 主HTML文件
├── css/
│   └── style.css          # 样式文件
├── js/
│   ├── config.js          # 配置文件
│   ├── data-loader.js     # 数据加载器
│   ├── card.js            # 卡片类
│   └── main.js            # 主程序
├── assets/
│   ├── 牌背.png           # 卡片背面图片
│   └── 牌面.png           # 卡片正面背景图片
└── data/
    └── card_data.csv      # 卡片数据文件
```

## 设计细节

### 1. 卡片布局

- 卡片默认以网格形式排列，根据配置文件中的设置自动调整每行卡片数量
- 卡片之间的间距和布局通过配置文件控制
- 响应式设计，会根据窗口大小自动调整布局

### 2. 卡片样式

- 卡片大小：默认宽度200px，高度300px（可在配置文件中修改）
- 卡片背面：显示「牌背.png」图片
- 卡片正面：由「牌面.png」作为背景，上面叠加三个文本框体
- 圆角设计：卡片四角为圆角，增加美观度
- 阴影效果：卡片有轻微阴影，增强立体感

### 3. 交互效果

- **悬停效果**：鼠标移动到卡片上时，卡片会向上浮动10px并增加阴影深度，提供视觉反馈
- **翻转效果**：点击卡片时，卡片会围绕Y轴旋转180度，实现3D翻转效果，展示卡片正面
- **再次点击**：点击已翻转的卡片，卡片会翻转回背面

### 4. 文本框体

- 每张卡片正面包含三个文本框体，位置、大小、字体样式等通过配置文件控制
- 文本框体背景为半透明白色，确保文字清晰可读
- 文本内容从CSV数据文件中随机获取，每个框体对应数据的一列

## 数据管理

### 配置文件 (config.js)

配置文件包含以下主要设置：

- **卡片数量**：页面加载时创建的卡片数量
- **卡片布局**：间距和每行卡片数量
- **卡片样式**：宽度和高度
- **文本框体**：每个框体的位置、大小、字体样式等
- **资源路径**：图片和数据文件的路径

### 数据文件 (card_data.csv)

数据文件采用CSV格式，每行代表一组数据，包含三个字段，分别对应卡片上的三个文本框体。当创建卡片时，系统会从数据文件中随机选择一组数据填充到卡片中。

## 操作逻辑

1. **页面加载**：
   - 加载配置文件和样式
   - 初始化数据加载器并读取CSV数据
   - 根据配置创建指定数量的卡片
   - 为每张卡片随机分配一组数据
   - 调整卡片布局

2. **用户交互**：
   - 鼠标悬停在卡片上时，卡片会有高亮浮动效果
   - 点击卡片时，卡片会翻转并展示正面内容
   - 再次点击已翻转的卡片，卡片会翻转回背面

3. **响应式调整**：
   - 窗口大小变化时，自动调整卡片布局，确保良好的显示效果

## 扩展性

本项目设计具有良好的扩展性：

- **配置驱动**：大部分样式和行为通过配置文件控制，无需修改代码
- **数据分离**：内容与展示分离，只需更新CSV数据文件即可更改卡片内容
- **模块化设计**：代码采用模块化设计，便于维护和扩展

## 使用说明

1. 确保所有文件按照文件结构放置正确
2. 根据需要修改配置文件 (config.js)
3. 准备CSV数据文件，确保格式正确
4. 通过Web服务器访问index.html文件
5. 点击卡片体验翻牌效果

## 注意事项

- 由于浏览器安全限制，本地直接打开HTML文件可能无法正常加载CSV数据，建议通过Web服务器访问
- 图片资源需要提前准备好并放置在assets目录下
- 确保CSV数据文件格式正确，否则可能导致卡片内容显示异常